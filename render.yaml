services:
  - type: web
    name: a-faheem
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /projects/*
        destination: /index.html
